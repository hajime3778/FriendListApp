export class Config {
  static domainUrl: string = "https://firestore.googleapis.com/v1/"
  static projectId: string = "friend-list-app-80ca0"
  static documentsPath: string = `projects/${Config.projectId}/databases/(default)/documents/`
}